# yaml-language-server: $schema=https://json.schemastore.org/kustomization
namespace: ingress-nginx
resources:
  - ../../../bases/ingress-nginx
patches: 
  - target:
      kind: HelmRelease
    patch: |
      - op: add 
        path: /spec/values/controller/kind
        value: DaemonSet
      # - op: add
      #   path: /spec/values/controller/ingressClassResource/default
      #   value: true