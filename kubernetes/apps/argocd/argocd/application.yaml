# yaml-language-server: $schema=https://raw.githubusercontent.com/mickkael/argo-events/JSONschema/api/jsonschema/schema.json

### ArgoCD Application ### 
# This points to kubernetes/argocd and allows for argocd to control itself. 

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "6"
spec:
  project: default
  source:
    repoURL: git@github.com:ashmantis1/ashman-homelab-ops.git
    targetRevision: HEAD
    path: kubernetes/argocd

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  #syncPolicy:
    #automated: # automated sync by default retries failed attempts 5 times with following delays between attempts ( 5s, 10s, 20s, 40s, 80s ); retry controlled using `retry` field.
     # prune: true # Specifies if resources should be pruned during auto-syncing ( false by default ).  
      #selfHeal: false # Specifies if resources should be self-healed 