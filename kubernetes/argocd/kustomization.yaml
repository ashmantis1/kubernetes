# yaml-language-server: $schema=https://json.schemastore.org/kustomization

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd
resources:
- namespace.yaml
- https://github.com/argoproj/argo-cd/manifests/cluster-install?ref=v2.8.0


patchesStrategicMerge:
  - ./config/argo-cm.yaml
  - ./config/argo-cmd-params-cm.yaml