apiVersion: v1
kind: Secret
metadata:
  name: object-store-config
  namespace: gitlab
stringData:
  config: |
    provider: AWS
    region: eu-west-1
    aws_access_key_id: 
    aws_secret_access_key: 
    aws_signature_version: 4
    endpoint:
    path_style: true
