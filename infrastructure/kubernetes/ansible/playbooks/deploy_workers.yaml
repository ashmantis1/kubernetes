- name: Join workers to cluster 
  hosts: workers
  become: true
  tasks:

    - name: Join cluster using generated command
      ansible.builtin.shell: "{{ hostvars[groups['bootstrap'][0]]['worker_join_command'] }}"
    - name: Sleep
      ansible.builtin.wait_for:
        timeout: 15